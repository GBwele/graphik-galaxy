<header>


<div class="space-tunnel">
	<div class="stars-wrapper"></div>
</div>
<div class="scanlines"></div>
<div class="vignette"></div>










<div id="logo-inscription" style="position: relative; z-index: 4;">
<!-- Bouton hamburger pour mobile -->

<button id="m_burger" class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">

	<i class="fa-solid fa-bars"></i>
</button>




		<div class="logo" id="haut">
			<a href="{{path('app_home')}}"><img src="../../assets/img/logo_official.png" alt="logo_du_site" class="logo_size"></a>

		</div>

		<div class="user">
			{% if app.user %}
				<h6>Bienvenue,
					{{app.user.username}}</h6>
				{% if is_granted('ROLE_ADMIN')  %}
					<a href="{{path('admin')}}">
						Admin
						<i class="fa-solid fa-user-shield"></i>
					</a>
				{% else %}
					<i class="fa-solid fa-user"></i>

				{% endif %}
			{% endif %}


			{# partie panier #}

			<a href="{{path('app_panier')}}">
				<i class="fa-solid fa-cart-shopping"></i>
				<span id="nb_article">{{app.session.get('nb') == 0 ? '' : app.session.get('nb')}}</span>
			</a>
			{% if app.user %}
				<li>
				
					<a href="{{path('app_logout')}}"><i class="fa-solid fa-right-from-bracket"></i></a>
				</li><i class="fa-solid fa-right-from-bracket"></i>
			{% else %}
				<li>
<a href="{{path('app_login')}}">
	<i class="fa-solid fa-right-to-bracket"></i></a>

				</li>
				<li>
					<a href="{{path('app_register')}}">s'enregistrer</a>
				</li>
			{% endif %}

		</div>
	</div>
	<hr>
<div class="random" style="position: relative; z-index: 4;">


		<div class="typing-effect">

			<span>{{ randomize.getRandomTagline() }}</span>

		</div>

	</div>

	<hr>



{# 
	<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
		<span class="navbar-toggler-icon"></span>
	</button> #}

	<!-- Ajout de la classe collapse navbar-collapse -->

	
<nav class="navbar navbar-expand-lg" style="position: relative; z-index: 4;">

		<div class="collapse navbar-collapse" id="navbarContent">
			<ul class="navbar-nav">


				<li>
					<a href="{{path('app_products',{categories: 'Mangas'})}}">Mangas</a>
				</li>
				<li>
					<a href="{{path('app_products',{categories: 'Goodies'})}}">Goodies</a>
				</li>
				<li>
					<a href="{{path('app_products',{categories: 'Comics'})}}">Comics</a>
				</li>
				<li>
					<a href="{{path('app_products',{categories: 'tout'})}}">Tout nos produits</a>
				</li>

			</ul>
		</div>
	</nav>
</header>
