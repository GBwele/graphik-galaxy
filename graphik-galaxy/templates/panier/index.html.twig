{% extends 'base.html.twig' %}

{% block title %}Votre Panier
{% endblock %}

{% block body %}
	{% if (app.user is empty) %}
		<div class="acces-interdit_non-utilisateur">
			<h2>acces interdit</h2>
			<p>vous devez etre un utilisateur pour acceder au panier...</p>
			<a href="{{path('app_login')}}">vous connecter</a>
			<a href="{{path('app_register')}}">vous inscrire</a>
		</div>
		{%else%}

		<div class="panier-presentation">

			<h2>Panier</h2>

			{% if (cart is empty) %}

				<p>Votre panier est vide...
					<br>
					voulez vous le remplir avec des
					<a href="{path(app_products)}">produits ?</a>
				</p>

			{% else %}
				<table>
					<thead>
						<tr>
							<th>Effacer</th>
							<th>Nom</th>
							<th>Quantité</th>
							<th>Prix</th>
							<th>Total</th>

						</tr>
					</thead>
					<tbody>
						{% for id, produit in cart %}
							<tr>
								<td>
									<i class="fa-solid fa-trash"></i>
								</td>
								<td>{{ produit.name }}</td>
								<td>XXX</td>

								{# <td>{{ produit.quantity }}</td> #}
								<td>{{ produit.name }}</td>
								<td>XXX</td>
								<td>XXX</td>


								{# <td>{{ produit.total }}</td> #}
								{# <td>{{ produit.delete }}</td> #}

							</tr>
						{% endfor %}
						<br><hr>
						{# <p>total des produits : <table><tbody><tr><td>{{ produit }}</td></tr></tbody></table></p> #}
					{% endif %}
				{% endif %}
				
			</tbody>


		</table>


	</div>
{% endblock %}
